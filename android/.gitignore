gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# La chiave di firma e' volutamente versionata: non protegge un segreto,
# serve solo a tenere stabile la firma dell'app fra una build e l'altra,
# altrimenti ogni APK risulterebbe di un autore diverso e non si potrebbe
# installare sopra il precedente. Vedi la nota nel README.
!app/vtm-signing.jks
